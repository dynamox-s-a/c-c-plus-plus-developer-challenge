#ifndef TYPEDEF_H_INCLUDED
#define TYPEDEF_H_INCLUDED

//GLOBAL DEFINES
enum operation{//operations defines
  SUM = 1,
  SUBTRACT,
  MULTIPLY,
  DIVIDE,
  DETERMINANT,
  //TODO: if new operation is added, add new enum here
  EXIT
};

#endif // TYPEDEF_H_INCLUDED
